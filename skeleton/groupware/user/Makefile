SUPDIR=/usr/local/apache/hotbiz/skeleton/blog/user

default:
all:
clean:
hbsync:
cartsync:
rsvsync:
dbbsync:
depend:
instdbb:
instcart:
instrsv:

insthb:
	install -c -m 644 -o root -g wheel *.skl ${SUPDIR}
