SUPDIR=/usr/local/apache/hotbiz/skeleton/blog/admin

default:
all:
clean:
depend:
hbsync:
cartsync:
rsvsync:
dbbsync:
instdbb:
instcart:
instrsv:

insthb:
	install -c -m 644 -o root -g wheel *.skl ${SUPDIR}
